# Shared

Reserved for code that is genuinely used by more than one game, or by the games and the arcade
(for example a common launcher contract or shared UI helpers).

Nothing lives here yet. Code is only moved here once at least two parts of the project actually need it.
